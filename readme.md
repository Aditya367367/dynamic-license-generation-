this is just a module of a project you have to create a full project to run this module 

install dependencies:
```bash
pip install -r requirements.txt
```
setup:
```bash
python manage.py migrate
python manage.py runserver
```
access http://127.0.0.1:8000/licenses/ to see the license management interface




